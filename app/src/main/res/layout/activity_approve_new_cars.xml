<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Admin.ApproveNewCarsActivity">

    <RelativeLayout
        android:id="@+id/Approve_relative"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/colorPrimary">

        <TextView
            android:id="@+id/Approve_Text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_centerVertical="true"
            android:text="Approving New Products "
            android:textColor="@color/White"
            android:textSize="25dp" />

    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/Approve_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/Approve_relative"
        android:layout_marginTop="1dp">

    </androidx.recyclerview.widget.RecyclerView>

</RelativeLayout>